require("dotenv").config(); const fs=require("fs"),path=require("path"); const {Pool}=require("pg"); const pool=new Pool({connectionString:process.env.DATABASE_URL,ssl:process.env.DB_SSL==="true"?{rejectUnauthorized:false}:false}); (async()=>{await pool.query("CREATE TABLE IF NOT EXISTS schema_migrations(filename TEXT PRIMARY KEY,applied_at TIMESTAMPTZ NOT NULL DEFAULT NOW())"); const dir=path.join(__dirname,"..","migrations"); for(const file of fs.readdirSync(dir).filter(x=>x.endsWith(".sql")).sort()){const done=await pool.query("SELECT 1 FROM schema_migrations WHERE filename=$1",[file]); if(done.rowCount)continue; await pool.query(fs.readFileSync(path.join(dir,file),"utf8")); await pool.query("INSERT INTO schema_migrations(filename) VALUES($1)",[file]); console.log("Applied",file)} await pool.end()})().catch(async e=>{console.error(e);try{await pool.end()}catch{}process.exit(1)});